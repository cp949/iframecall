{
  "name": "iframecall-repo",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "build:release": "turbo run build --force",
    "dev": "turbo run dev",
    "dev:r19": "turbo run dev --filter=host-r19 --filter=iframe-r19",
    "dev:r18": "turbo run dev --filter=host-r18 --filter=iframe-r18",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "verify": "pnpm lint && pnpm build && pnpm check-types && pnpm test",
    "verify:release": "turbo run lint --force && pnpm build:release && turbo run check-types --force && turbo run test --force",
    "release-it": "cd packages/iframecall && release-it"
  },
  "devDependencies": {
    "@release-it/keep-a-changelog": "8.0.1",
    "prettier": "3.9.8",
    "release-it": "21.1.0",
    "turbo": "^2.10.11",
    "typescript": "6.0.3"
  },
  "packageManager": "pnpm@11.27.1",
  "engines": {
    "node": ">=22.13.0"
  }
}
